spring:
  application:
    name: sc-pluto

  cloud:
    config:
#      uri: http://localhost:9999
      discovery:
        enabled: true
        serviceId: sc-earth-config-server
      profile: pre
      label: master

management:
  security:
    enabled: true


